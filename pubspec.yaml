name: biometric
description: A biometric service provider module.
version: 1.0.2+4
publish_to: none

environment:
  sdk: '>=3.5.0'

dependencies:
  flutter:
    sdk: flutter
  local_auth: ^2.3.0
  local_auth_darwin: ^1.4.3
  flutter_secure_storage: ^9.2.4
  meta: ^1.15.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.14
  mockito: ^5.3.2
  build_runner: ^2.4.9